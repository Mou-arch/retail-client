import Taro, {Component} from '@tarojs/taro'
import {View} from "@tarojs/components";
import './index.scss';

export default class Segmentation extends Component {
  render() {
    return (
      <View className='segmentation' style={{padding: '0 10px'}} />
    );
  }
}
